%table{ border: '0', cellpadding: '0', cellspacing: '0' }
  %thead
    %tr
      %th Name
      %th Email Address
  %tbody
    - @users.each do |user|
      %tr
        %td= h(user.name)
        %td= h(user.email)
